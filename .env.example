# OpenAI API Configuration
# Add your key to connect to OpenAI (falls back to MockLLMClient if empty)
OPENAI_API_KEY=your_openai_api_key_here

# SQLite Database Settings
# Set path to SQLite database. Use ":memory:" for transient testing, or "interview_bot.db" for persistent local.
DB_PATH=interview_bot.db
